version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/kd-backend/
hooks:
  BeforeInstall:
    - location: scripts/install_dependencies
      timeout: 300
      runas: ubuntu
    - location: scripts/start_server
      timeout: 300
      runas: ubuntu
